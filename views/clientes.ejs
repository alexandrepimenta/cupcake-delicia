<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="utf-8">
  <title>Clientes - Cupcake Delícia</title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <header class="cabecalho">
    <h1 class="logo">🧁 Cupcake Delícia</h1>
    <nav class="menu">
      <a href="/">Home</a>
      <a href="/cupcakes">Cupcakes</a>
      <a href="/clientes">Clientes</a>
      <a href="/pedidos">Pedidos</a>
    </nav>
  </header>

  <main class="conteudo">
    <h2>Lista de Clientes</h2>

    <a href="/clientes/novo" class="botao-pequeno">+ Novo Cliente</a>

    <% if (clientes && clientes.length) { %>
      <div class="lista">
        <% clientes.forEach(function(c){ %>
          <div class="item-card">
            <h3><%= c.nome %></h3>
            <p><strong>Email:</strong> <%= c.email %></p>
            <p><strong>Telefone:</strong> <%= c.telefone %></p>
            <p><strong>Endereço:</strong> <%= c.endereco %></p>

            <!-- Botão de exclusão -->
            <form method="POST" action="/clientes/excluir/<%= c.id_cliente %>" onsubmit="return confirm('Tem certeza que deseja excluir este cliente?');">
              <button type="submit" class="btn-excluir">Excluir</button>
            </form>
          </div>
        <% }) %>
      </div>
    <% } else { %>
      <p>Nenhum cliente cadastrado ainda.</p>
    <% } %>
  </main>

  <footer class="rodape">
    <p>© 2025 Cupcake Delícia | Desenvolvido por Alexandre Pimenta</p>
  </footer>
</body>
</html>